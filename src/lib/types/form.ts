export type menu = {
    id: number,
    key: string,
    name: string,
    module: string,
    icon: string,
    order: number,
    route: string,
    parent_key: string,
    is_menu: boolean,
    is_crud: boolean,
    created_at: string
}